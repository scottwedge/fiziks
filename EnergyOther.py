 # Energy Others
 
 from sympy import *
 
  m =  2 * ke / (v**2) # mass with ke
  m = pe / g / h # mass with peg
  v = (2 * ke / m)**.5
  h = peGravitational / m / g
  k = peSpring * 2 / (x**2)
  x = (peSpring * 2 /k)**.5
  